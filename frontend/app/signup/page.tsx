"use client";

import Link from "next/link";
import { FormEvent, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, AtSign, Check, LockKeyhole, Mail, UserRound } from "lucide-react";
import { register } from "@/utils/auth-api";
import { getApiErrorMessage } from "@/utils/api-error";
import { useNotification } from "@/context/notification-context";

const makeSlug = (value: string) => value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");

export default function SignUpPage() {
  const router = useRouter(); const { success, error } = useNotification();
  const [username, setUsername] = useState(""); const [email, setEmail] = useState(""); const [profileSlug, setProfileSlug] = useState(""); const [password, setPassword] = useState(""); const [loading, setLoading] = useState(false);
  const strength = useMemo(() => [password.length >= 6, /[A-Za-z]/.test(password), /\d/.test(password), /[@$!%*#?&]/.test(password)].filter(Boolean).length, [password]);
  const handleSubmit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); const slug = makeSlug(profileSlug || username);
    if (username.trim().length < 3) return error("Your display name needs at least 3 characters.");
    if (!email.trim() || !slug) return error("Add your email and a unique profile address.");
    if (strength < 4) return error("Use 6+ characters with a letter, number, and special character.");
    try { setLoading(true); await register({ username: username.trim(), email: email.trim(), password, profileSlug: slug }); success("Your ProofFolio is ready. Sign in to begin."); router.push("/signin"); }
    catch (err) { error(getApiErrorMessage(err, "We could not create your account.")); } finally { setLoading(false); }
  };
  const fieldClass = "h-12 w-full rounded-xl border border-white/10 bg-[#0b1020] pl-11 pr-4 text-sm text-white outline-none transition placeholder:text-slate-600 focus:border-lime-300/60 focus:ring-4 focus:ring-lime-300/10";
  return <main className="flex flex-1 bg-[#0a0d18] px-5 py-10 sm:px-8 lg:items-center lg:py-14"><section className="mx-auto grid w-full max-w-5xl overflow-hidden rounded-[2rem] border border-white/10 bg-[#111728] shadow-2xl shadow-black/40 lg:grid-cols-[1.08fr_.92fr]"><div className="p-7 sm:p-10 lg:p-14"><div className="mx-auto max-w-md"><p className="text-xs font-bold uppercase tracking-[.18em] text-fuchsia-300">Set up your proof space</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.055em] text-white">Let your results introduce you.</h1><p className="mt-2 text-sm leading-6 text-slate-400">Create the home for work you&apos;re proud of and testimonials you&apos;ve earned.</p><form className="mt-7 space-y-4" onSubmit={handleSubmit} noValidate><label className="block text-sm font-semibold text-slate-200">Display name<div className="relative mt-2"><UserRound className="pointer-events-none absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><input value={username} onChange={(event) => { setUsername(event.target.value); if (!profileSlug) setProfileSlug(makeSlug(event.target.value)); }} autoComplete="name" placeholder="Areeba Shah" className={fieldClass} /></div></label><label className="block text-sm font-semibold text-slate-200">Email address<div className="relative mt-2"><Mail className="pointer-events-none absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><input value={email} onChange={(event) => setEmail(event.target.value)} type="email" autoComplete="email" placeholder="you@studio.com" className={fieldClass} /></div></label><label className="block text-sm font-semibold text-slate-200">Your public address<div className="relative mt-2"><AtSign className="pointer-events-none absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><input value={profileSlug} onChange={(event) => setProfileSlug(makeSlug(event.target.value))} placeholder="areeba-shah" className={fieldClass} /></div><span className="mt-1.5 block text-xs font-normal text-slate-500">prooffolio.app/profile/{makeSlug(profileSlug || username) || "your-name"}</span></label><label className="block text-sm font-semibold text-slate-200">Password<div className="relative mt-2"><LockKeyhole className="pointer-events-none absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-500" /><input value={password} onChange={(event) => setPassword(event.target.value)} type="password" autoComplete="new-password" placeholder="6+ characters" className={fieldClass} /></div><div className="mt-2 flex gap-1">{Array.from({ length: 4 }).map((_, index) => <span key={index} className={`h-1 flex-1 rounded-full ${index < strength ? "bg-lime-300" : "bg-white/10"}`} />)}</div><span className="mt-1.5 block text-xs font-normal text-slate-500">Letter, number, and special character required.</span></label><button disabled={loading} className="mt-2 flex h-12 w-full items-center justify-center gap-2 rounded-xl bg-lime-300 text-sm font-bold text-[#101424] transition hover:bg-lime-200 disabled:cursor-not-allowed disabled:opacity-60">{loading ? "Creating your space…" : <>Create ProofFolio <ArrowRight className="h-4 w-4" /></>}</button></form><p className="mt-7 text-center text-sm text-slate-400">Already collecting proof? <Link href="/signin" className="font-bold text-lime-300 hover:text-lime-200">Sign in</Link></p></div></div><aside className="relative hidden overflow-hidden bg-[#28204c] p-10 lg:flex lg:flex-col lg:justify-between"><div className="absolute -right-24 -top-20 h-64 w-64 rounded-full bg-fuchsia-400/35 blur-2xl" /><div className="absolute -bottom-24 -left-20 h-64 w-64 rounded-full bg-lime-300/20 blur-2xl" /><div className="relative"><p className="text-xs font-bold uppercase tracking-[.18em] text-fuchsia-200">From work to proof</p><h2 className="mt-5 max-w-xs text-5xl font-semibold leading-[.94] tracking-[-.07em] text-white">A portfolio people can believe.</h2></div><div className="relative rounded-2xl border border-white/15 bg-white/10 p-5 backdrop-blur"><div className="flex items-center gap-2 text-lime-200"><Check className="h-4 w-4" /> <p className="text-sm font-bold">Verified testimonial flow</p></div><p className="mt-3 text-sm leading-6 text-slate-300">Clients write freely. You decide what appears on your public story.</p></div></aside></section></main>;
}
